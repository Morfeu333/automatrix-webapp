# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build output
build
.next
out

# Misc
.cache
.vite
.turbo
*.tsbuildinfo

# Serena tool config
.serena/

# Supabase CLI temp
supabase/.temp/

# Old code (preserved locally, excluded from repo)
src-old/
backend/

# SQLite databases
*.db
*.db-shm
*.db-wal

# HTML previews (design docs, not app code)
docs/previews/

# N8N workflow files (too large for git)
public/n8n-workflows/

# Design reference files (local only)
CPV2.png
*.pen

# Video files (too large for git)
*.mp4
*.mov
*.avi
*.mkv

# Video scripts and transcriptions (marketing content, not app code)
docs/video-scripts/

# Onboarding HTML prototypes (design docs, not app code)
onboarding/
